root = "."
tmp_dir = "tmp"

[build]
cmd = "go run ./cmd/main.go"
entrypoint = ["go", "run", "./cmd/main.go"]  # NEW: Replace bin
delay = 500
stop_on_error = true

poll = true
poll_interval = 200

exclude_dir = ["tmp"]

[log]
time = true
